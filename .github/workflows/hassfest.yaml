name: Hassfest

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - develop

jobs:
  hassfest:
    name: Hassfest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Hassfest
        uses: home-assistant/actions/hassfest@master 